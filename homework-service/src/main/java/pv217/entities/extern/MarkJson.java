package pv217.entities.extern;

import pv217.entities.Mark;

// made for easier manipulation with marks (parsing enum), we don't care about performance
public class MarkJson {
    public Mark mark;
}
